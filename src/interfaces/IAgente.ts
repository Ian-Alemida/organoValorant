export default interface IAgente {
    classe?: string,
    favorito?: boolean,
    nome: string,
    imagem?: string,
    funcao?: string,
    id: string,
    key?: string | React.Key | null | undefined
}